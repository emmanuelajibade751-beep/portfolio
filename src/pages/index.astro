---
/**
 * Landing page — reads as a practice overview, not a project dump. The three
 * signature canvas scenes (morph motif, flow field, parametric playground)
 * moved to /design/: on the landing page they made the site read as a
 * computational playground; here the PracticeStrip signals breadth first,
 * then a curated FeaturedWork row, then about/contact.
 */
import Base from "../layouts/Base.astro";
import DotGridHero from "../components/DotGridHero.astro";
import PracticeStrip from "../components/PracticeStrip.astro";
import FeaturedWork from "../components/FeaturedWork.astro";
import AboutContact from "../components/AboutContact.astro";
---

<Base
  title="Sunday Emmanuel Ajibade — Architecture, Computation & Research"
  description="Portfolio of Sunday Emmanuel Ajibade: architectural design, computational systems, structural detailing, research, and AI-assisted workflows."
>
  <!-- Cursor-reactive dot grid -->
  <DotGridHero />

  <!-- Four discipline tiles — the fastest signal of practice breadth -->
  <PracticeStrip />

  <!-- Curated cross-discipline work (featured: true), not the full catalogue -->
  <FeaturedWork />

  <!-- About + contact -->
  <AboutContact />
</Base>
